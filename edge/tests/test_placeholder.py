"""P0 skeleton test for the edge package.

Exists so the pytest wiring is real and CI is green. Real edge tests
(drivers, sampler, buffer) arrive with the code in P1.
"""


def test_placeholder() -> None:
    assert True
