import { describe, it, expect } from "vitest";

// P0 skeleton test — keeps Vitest wiring real. Real component/hook tests
// (WebSocket store, charts, panels) arrive with the app in P5.
describe("placeholder", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
