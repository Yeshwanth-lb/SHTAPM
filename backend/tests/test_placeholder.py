"""P0 skeleton test for the backend package.

Keeps pytest wiring real and CI green. Real backend tests (API, WS, MQTT,
ledger verify, auth/RBAC/RLS) arrive with the code in P4.
"""


def test_placeholder() -> None:
    assert True
