# SHTAPM backend runtime dependencies (frozen stack, TRD §02.2).
# Active from P0/M3.3 (contract + MQTT telemetry ingestion):
pydantic==2.*        # shared contract (D006/D007)
fastapi==0.110.*     # backend framework + lifespan
uvicorn==0.29.*      # ASGI server
paho-mqtt==1.6.*     # MQTT telemetry consumer

# Deferred to P4 (persistence + auth), pinned then:
# sqlalchemy==2.0.*, alembic, psycopg[binary], python-jose, passlib[bcrypt]
